- if instance.errors.any?
  .errors
    %strong
      = pluralize(instance.errors.count, "error")
    prohibited this from being saved:"
    %ul
      - instance.errors.full_messages.each do |msg|
        %li= msg
