= render :partial => 'master_texts/tabs'

.list-group
  - @views.each do |view|
    = link_to view, class: "list-group-item clearfix view" do
      .text-primary
        %i.fa.fa-list-alt
        = view.name
      %small.comments= view.comments
  - if current_user.is_developer?
    .list-group-item.clearfix
      = link_to 'New View', new_project_view_path(project_id: @project.id), class: 'btn btn-default pull-right'
