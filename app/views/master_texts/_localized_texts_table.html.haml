%table.table.table-striped
  %thead
    %tr
      %th Language
      %th Localized Text
      %th Needs review
  %tbody
    - localized_texts.includes(project_language: :language).order('languages.name asc').references('languages').each do |localized_text|
      %tr[localized_text]
        %td
          %p= localized_text.language
        %td
          - if localized_text.pluralizable
            = localized_text.other
          - else
            = localized_text.text
          = link_to edit_localized_text_path(localized_text) do
            [ Edit ]

        %td
          .checkbox
            %label
              - if localized_text.needs_review
                .badge needs review
